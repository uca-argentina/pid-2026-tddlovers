# Place this in the ROOT of your frontend repo as "nginx.conf"
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Falls back to index.html so React Router routes work on refresh
        try_files $uri /index.html;
    }
}
